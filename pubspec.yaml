name: weather
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  weather_icons: ^3.0.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.2
  intl: ^0.20.2




dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:

  uses-material-design: true

flutter_icons:
  android: true
  ios: false
  image_path: "assets/icon.png"  # Change to your icon path
  adaptive_icon_foreground: "assets/icon_foreground.png"
  adaptive_icon_background: "assets/icon_background.png"